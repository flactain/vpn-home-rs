use crate::entities::error::AppError;

pub async fn create_client() -> Result<(), AppError> {
    //TODO:mail
    // let email = Message::builder()
    Ok(())
}

pub async fn approve_client() -> Result<(), AppError> {
    // repository.Approve_request

    // wireguard restruct!

    Ok(())
}
